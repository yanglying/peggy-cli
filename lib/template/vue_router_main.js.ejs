import { createRouter, createWebHistory } from 'vue-router'
const routes = [ <%
routes.forEach(function(route) { %> route <% }) %> ]; const router=createRouter({ history:
createWebHistory(), routes:routes }) export default router
